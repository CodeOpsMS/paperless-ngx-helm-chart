# Artifact Hub assigns a unique repository ID during registration.
#
# 1. Publish this chart through GitHub Pages or another Helm repository.
# 2. Register that repository at https://artifacthub.io/.
# 3. Add the assigned ID below as `repositoryID`.
#
# Do not reuse the ID from the original WrenIX repository.

owners:
  - name: M.Lämmerzahl
    email: codeopsms@users.noreply.github.com
